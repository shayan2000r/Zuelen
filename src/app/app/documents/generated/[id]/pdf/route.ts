import { buildFinancialDocumentPdf } from "@/lib/financial-document-pdf";
import { createClient } from "@/lib/supabase/server";
import { getWorkspace } from "@/lib/workspace";

export const dynamic="force-dynamic";
export const runtime="nodejs";
function safeName(value:string){return value.normalize("NFKD").replace(/[^a-zA-Z0-9-_ ]+/g,"").trim().replace(/\s+/g,"-").slice(0,100)||"financial-document"}
function asObject(value:unknown){return value&&typeof value==="object"?value as Record<string,unknown>:null}
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,workspace=await getWorkspace();if(!workspace.authenticated||!workspace.company)return new Response("Unauthorized",{status:401});const supabase=await createClient(),{data,error}=await supabase.from("generated_documents").select("id,title,document_type,payload,fiscal_year").eq("id",id).eq("company_id",workspace.company.id).maybeSingle();if(error)return new Response(error.message,{status:500});if(!data)return new Response("Not found",{status:404});try{const payload=data.payload as Record<string,unknown>,company=asObject(payload.company),frozenPath=typeof company?.brand_image_path==="string"?company.brand_image_path:null,brandPath=frozenPath||workspace.company.brand_image_path;let brandAsset:{bytes:Uint8Array;mimeType:string}|null=null;if(brandPath){const{data:brand}=await supabase.storage.from("company-documents").download(brandPath);if(brand){brandAsset={bytes:new Uint8Array(await brand.arrayBuffer()),mimeType:brand.type||(/\.png$/i.test(brandPath)?"image/png":/\.jpe?g$/i.test(brandPath)?"image/jpeg":"application/octet-stream")}}}const bytes=await buildFinancialDocumentPdf(payload,data.document_type,data.title,brandAsset);return new Response(Buffer.from(bytes),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename=\"${safeName(data.title)}.pdf\"`,"Cache-Control":"private, no-store"}})}catch(error){return new Response(error instanceof Error?error.message:"PDF generation failed",{status:500})}}