import { ArrowDownLeft, ArrowUpRight, Search } from "lucide-react";
import { redirect } from "next/navigation";
import { SourceTransactionForm } from "@/components/source-transaction-form";
import { TransactionReviewCard } from "@/components/transaction-review-card";
import { TransactionRowActions } from "@/components/transaction-row-actions";
import { TransactionBulkActions } from "@/components/transaction-bulk-actions";
import styles from "@/components/live.module.css";
import { createClient } from "@/lib/supabase/server";
import { getWorkspace } from "@/lib/workspace";

export const dynamic = "force-dynamic";
function money(value:number,currency:string){return new Intl.NumberFormat("en-LU",{style:"currency",currency,minimumFractionDigits:2}).format(value)}
type SearchParams=Promise<{q?:string;status?:string}>;
export default async function TransactionsPage({searchParams}:{searchParams:SearchParams}){
 const params=await searchParams,q=(params.q??"").trim().toLowerCase(),status=params.status??"all";const workspace=await getWorkspace();if(!workspace.authenticated)redirect("/sign-in");if(!workspace.company)redirect("/setup");const supabase=await createClient();
 const[{data,error},{data:accountData,error:accountError}]=await Promise.all([
  supabase.from("source_transactions").select("id,occurred_on,direction,amount_gross,amount_net,vat_amount,vat_rate,vat_treatment,counterparty_country,currency,counterparty_name,description,classification_status,posted_journal_entry_id,suggested_account_id,suggestion_confidence,suggestion_reason,suggestion_kind").eq("company_id",workspace.company.id).not("classification_status","in",'(reversed,ignored)').order("occurred_on",{ascending:false}).order("created_at",{ascending:false}).limit(200),
  supabase.from("company_accounts").select("id,code,label,account_type,is_active").eq("company_id",workspace.company.id).eq("is_active",true).order("code",{ascending:true})]);
 if(error)throw new Error(`Could not load transactions: ${error.message}`);if(accountError)throw new Error(`Could not load accounting categories: ${accountError.message}`);const rows=data??[],accounts=(accountData??[]).filter(a=>["expense","revenue","asset","liability"].includes(a.account_type));
 const postedIds=rows.map(r=>r.posted_journal_entry_id).filter((id):id is string=>Boolean(id)),entryNumbers=new Map<string,number>();if(postedIds.length){const{data:entries}=await supabase.from("journal_entries").select("id,entry_number").in("id",postedIds);for(const e of entries??[])entryNumbers.set(e.id,Number(e.entry_number))}
 const pendingRows=rows.filter(r=>["unclassified","review","classified"].includes(r.classification_status)),nextReview=pendingRows[0]??null,suggestedCount=pendingRows.filter(r=>r.suggested_account_id&&Number(r.suggestion_confidence??0)>=.7).length,unresolvedCount=pendingRows.length-suggestedCount;
 const visibleRows=rows.filter(r=>{const statusMatch=status==="all"||(status==="posted"?r.classification_status==="posted":status==="review"?r.classification_status!=="posted":true);if(!statusMatch)return false;if(!q)return true;return[r.counterparty_name,r.description,r.currency,r.classification_status,r.vat_treatment,r.counterparty_country].filter(Boolean).join(" ").toLowerCase().includes(q)});
 return <div className={styles.liveWrap}><div className={styles.liveIntro}><div><p className={styles.eyebrow}>Bookkeeping source</p><h1>Transactions</h1><p>Review, classify and post activity without losing the audit trail.</p></div><span className={styles.liveBadge}><span>{pendingRows.length}</span> to review · {rows.length} active</span></div><SourceTransactionForm/>{pendingRows.length?<div style={{marginBottom:14}}><TransactionBulkActions suggestedCount={suggestedCount} unresolvedCount={unresolvedCount}/></div>:null}<section className={`${styles.transactionLayout} compta-single-column`}><div className={styles.transactionRight}>{nextReview?<TransactionReviewCard transaction={nextReview} accounts={accounts}/>:<article className={styles.reviewCard}><div className={styles.emptyState}><h3>Accounting inbox cleared.</h3><p>Every recorded transaction has been classified and posted. New activity will appear here for review.</p></div></article>}<article className={`${styles.ledgerPanel} compta-activity-panel`}><div className={styles.ledgerHead}><div><p className={styles.eyebrow}>Activity log</p><h2>Recorded transactions</h2></div><span>{visibleRows.length} shown · newest first</span></div><form className="compta-list-tools" method="get"><label><Search size={15}/><input name="q" defaultValue={params.q??""} placeholder="Search transactions"/></label><select name="status" defaultValue={status}><option value="all">All statuses</option><option value="review">Needs review</option><option value="posted">Posted</option></select><button type="submit">Apply</button></form>{visibleRows.length===0?<div className={styles.emptyState}><h3>{rows.length?"No matching transactions.":"No transactions yet."}</h3><p>{rows.length?"Try a different search or status filter.":"Use the floating action button or import a bank statement to create the first activity."}</p></div>:<table className={`${styles.ledgerTable} compta-activity-table`}><thead><tr><th>Date</th><th>Transaction</th><th>Status</th><th>Amount</th><th aria-label="Actions"/></tr></thead><tbody>{visibleRows.map(row=>{const income=row.direction==="income",entryNumber=row.posted_journal_entry_id?entryNumbers.get(row.posted_journal_entry_id):undefined,suggested=row.suggested_account_id?accounts.find(a=>a.id===row.suggested_account_id):undefined,treatment=row.vat_treatment&&row.vat_treatment!=="domestic"&&row.vat_treatment!=="unknown"?` · ${row.vat_treatment.replaceAll("_"," ")}`:"";return <tr key={row.id}><td>{new Date(`${row.occurred_on}T12:00:00`).toLocaleDateString("en-LU",{day:"2-digit",month:"short"})}</td><td className={styles.ledgerName}><strong>{row.counterparty_name||row.description||(row.suggestion_kind==="refund_candidate"?"Possible refund":income?"Income":"Expense")}</strong><small>{row.classification_status!=="posted"&&suggested?`${row.suggestion_kind==="refund_candidate"?"Possible refund · ":""}Suggested ${suggested.code} · ${suggested.label} · ${Math.round(Number(row.suggestion_confidence??0)*100)}%${treatment}`:row.amount_net!==null?`Net ${money(Number(row.amount_net),row.currency)}${row.vat_amount?` · VAT ${money(Number(row.vat_amount),row.currency)}`:""}${treatment}`:""}</small></td><td><span className={`${styles.rowStatus} ${row.classification_status==="posted"?styles.postedStatus:""}`}>{row.classification_status==="posted"?`Posted${entryNumber?` · J${String(entryNumber).padStart(4,"0")}`:""}`:suggested?"Suggested":"Review"}</span></td><td className={income?styles.incomeAmount:styles.expenseAmount}>{income?<ArrowUpRight size={12}/>:<ArrowDownLeft size={12}/>} {income?"+":"−"}{money(Number(row.amount_gross),row.currency)}</td><td><TransactionRowActions row={row} accounts={accounts}/></td></tr>})}</tbody></table>}</article></div></section></div>;
}
