"use client";
import{Bell,BookOpen,Building2,ChevronDown,CircleHelp,FileCheck2,FileText,Landmark,LayoutDashboard,Menu,ReceiptText,Search,Settings,ShieldCheck,WalletCards,X}from"lucide-react";import Link from"next/link";import{usePathname}from"next/navigation";import{useMemo,useState,type ReactNode}from"react";import styles from"./live.module.css";
const nav=[{label:"Overview",icon:LayoutDashboard,href:"/app"},{label:"Transactions",icon:WalletCards,href:"/app/transactions"},{label:"Invoices",icon:ReceiptText,href:"/app/invoices"},{label:"Accounting",icon:BookOpen,href:"/app/accounting"},{label:"Taxes",icon:Landmark,href:"/app/taxes"},{label:"Compliance",icon:FileCheck2,href:"/app/compliance"},{label:"Documents",icon:FileText,href:"/app/documents"}]as const;
function initials(value:string){return value.split(/\s+/).filter(Boolean).slice(0,2).map(part=>part[0]?.toUpperCase()).join("")}
export function AppFrame({children,companyName,fiscalYear,email,attentionCount=0}:{children:ReactNode;companyName:string;fiscalYear:number;email:string|null;attentionCount?:number}){const pathname=usePathname(),[mobileNav,setMobileNav]=useState(false),userLabel=useMemo(()=>email?.split("@")[0]??"Owner",[email]);const section=pathname.startsWith("/app/transactions")?"Transactions":pathname.startsWith("/app/invoices")?"Invoices":pathname.startsWith("/app/accounting")?"Accounting":pathname.startsWith("/app/taxes")?"Taxes":pathname.startsWith("/app/compliance")?"Compliance":pathname.startsWith("/app/documents")?"Documents":"Overview";return <main className="app-shell"><aside className={`sidebar ${mobileNav?"sidebar-open":""}`}><div className="brand-row"><Link href="/app" className={styles.brandLink} aria-label="Compta overview"><div className="brand-mark"><span>C</span></div><div className="brand-word">Compta</div></Link><button className="icon-btn mobile-only" onClick={()=>setMobileNav(false)} aria-label="Close navigation"><X size={18}/></button></div><button className="company-switcher" type="button"><span className="company-avatar">{initials(companyName).slice(0,1)||"C"}</span><span className="company-copy"><strong>{companyName}</strong><small>{fiscalYear} financial year</small></span><ChevronDown size={16}/></button><nav className="nav-list" aria-label="Primary navigation">{nav.map(item=>{const active=item.href==="/app"?pathname==="/app":pathname.startsWith(item.href);return <Link key={item.label} href={item.href} className={`nav-item ${active?"nav-active":""}`} onClick={()=>setMobileNav(false)}><item.icon size={18} strokeWidth={1.8}/><span>{item.label}</span>{item.label==="Transactions"&&attentionCount>0?<em>{attentionCount}</em>:null}</Link>})}</nav><div className="sidebar-spacer"/><div className="sidebar-insight"><div className="insight-icon"><ShieldCheck size={18}/></div><div><strong>Live workspace</strong><p>Protected by company-level RLS.</p></div><span className="health-dot"/></div><div className="nav-list nav-list-bottom"><span className={`nav-item ${styles.disabledNav}`}><CircleHelp size={18}/><span>Help center</span></span><span className={`nav-item ${styles.disabledNav}`}><Settings size={18}/><span>Settings</span></span></div><div className="user-row"><div className="user-avatar">{initials(userLabel)||"O"}</div><div><strong>{userLabel}</strong><small>Owner</small></div><form action="/auth/signout" method="post" className={styles.signOutForm}><button type="submit" className={styles.signOutButton}>Sign out</button></form></div></aside>{mobileNav?<button className="scrim" aria-label="Close navigation" onClick={()=>setMobileNav(false)}/>:null}<section className="workspace"><header className="topbar"><div className="topbar-left"><button className="icon-btn mobile-only" onClick={()=>setMobileNav(true)} aria-label="Open navigation"><Menu size={19}/></button><div className="crumb"><Building2 size={15}/><span>{companyName}</span><span>/</span><strong>{section}</strong></div></div><div className="topbar-actions"><button className="search-btn" type="button"><Search size={16}/><span>Search Compta</span><kbd>⌘ K</kbd></button><button className="icon-btn" type="button" aria-label="Notifications"><Bell size={18}/>{attentionCount>0?<span className="notification-dot"/>:null}</button></div></header>{children}</section></main>}
