"use client";
import{CheckCircle2,LoaderCircle,RefreshCw}from"lucide-react";import{useActionState}from"react";import{syncComplianceCalendar,type ComplianceState}from"@/app/app/compliance/actions";import{useI18n}from"@/components/locale-context";import{useRolePermissions}from"@/components/role-context";import styles from"./compliance-sync-button.module.css";
const initial:ComplianceState={status:"idle",message:""};
export function ComplianceSyncButton({year}:{year:number}){const{canAccount}=useRolePermissions(),{locale}=useI18n(),fr=locale==="fr",[state,action,pending]=useActionState(syncComplianceCalendar,initial);if(!canAccount)return null;return <form action={action} className={styles.syncForm}><input type="hidden" name="year" value={year}/><button type="submit" disabled={pending}>{pending?<LoaderCircle className={styles.spin} size={14}/>:<RefreshCw size={14}/>} {fr?"Actualiser les règles":"Refresh rules"}</button>{state.message?<span className={state.status==="error"?styles.syncError:styles.syncSuccess}>{state.status==="success"?<CheckCircle2 size={12}/>:null}{state.message}</span>:null}</form>}
