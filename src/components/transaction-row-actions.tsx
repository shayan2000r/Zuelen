"use client";

import { Calculator, LoaderCircle, Pencil, RefreshCcw, Trash2, X } from "lucide-react";
import { useRouter } from "next/navigation";
import { useActionState, useEffect, useMemo, useRef, useState } from "react";
import { deleteSourceTransactionAction, editSourceTransactionAction, recategorizeSourceTransactionAction, type TransactionActionState } from "@/app/app/transactions/actions";
import { TransactionEvidenceAction } from "@/components/transaction-evidence-action";
import { TransactionHistoryAction } from "@/components/transaction-history-action";
import styles from "./record-actions.module.css";

type Account={id:string;code:string;label:string;account_type:string};
type Evidence={id:string;file_name:string;type:string;file_size:number|null;created_at:string};
type AuditEvent={id:number;event_type:string;actor_user_id:string|null;actor_name:string|null;metadata:Record<string,unknown>;created_at:string};
type Row={id:string;occurred_on:string;direction:string;amount_gross:number|string;amount_net?:number|string|null;vat_amount:number|string|null;vat_rate?:number|string|null;vat_treatment?:string|null;counterparty_country?:string|null;counterparty_name:string|null;description:string|null;classification_status:string;suggested_account_id?:string|null;currency?:string;evidence?:Evidence[];history?:AuditEvent[]};
const initial:TransactionActionState={status:"idle",message:""};
function inferredRate(gross:number,vat:number){const net=gross-vat;if(vat<=0||net<=0)return 0;const actual=vat/net*100;return[17,14,8,3].reduce((best,r)=>Math.abs(r-actual)<Math.abs(best-actual)?r:best,17)}
function money(v:number){return new Intl.NumberFormat("en-LU",{style:"currency",currency:"EUR",minimumFractionDigits:2}).format(v||0)}

export function TransactionRowActions({row,accounts=[]}:{row:Row;accounts?:Account[]}){
 const dialog=useRef<HTMLDialogElement>(null),router=useRouter();
 const[editState,editAction,editPending]=useActionState(editSourceTransactionAction,initial),[deleteState,deleteAction,deletePending]=useActionState(deleteSourceTransactionAction,initial),[categoryState,categoryAction,categoryPending]=useActionState(recategorizeSourceTransactionAction,initial);
 const gross=Number(row.amount_gross),existingVat=Number(row.vat_amount??0),defaultRate=Number(row.vat_rate??inferredRate(gross,existingVat)),defaultTreatment=row.vat_treatment??"domestic";
 const[amount,setAmount]=useState(gross),[rate,setRate]=useState(defaultRate),[included,setIncluded]=useState(true),[treatment,setTreatment]=useState(defaultTreatment);
 const calc=useMemo(()=>{if(!amount)return{net:0,vat:0,gross:0};if(treatment==="eu_b2b_reverse_charge"){const vat=amount*rate/100;return{net:amount,vat,gross:amount}}if(treatment==="non_eu"||treatment==="exempt_or_zero"||rate===0)return{net:amount,vat:0,gross:amount};if(included){const net=amount/(1+rate/100);return{net,vat:amount-net,gross:amount}}const vat=amount*rate/100;return{net:amount,vat,gross:amount+vat}},[amount,rate,included,treatment]);
 const posted=row.classification_status==="posted",eligible=accounts.filter(a=>row.direction==="income"?["revenue","asset","liability","expense"].includes(a.account_type):["expense","asset","liability"].includes(a.account_type)).filter(a=>!["5131","421611","461411"].includes(a.code));
 useEffect(()=>{if(editState.status==="success"||categoryState.status==="success"){dialog.current?.close();router.refresh()}},[editState.status,categoryState.status,router]);useEffect(()=>{if(deleteState.status==="success")router.refresh()},[deleteState.status,router]);
 return <div className={styles.actions} onClick={event=>event.stopPropagation()} onKeyDown={event=>event.stopPropagation()}><TransactionEvidenceAction transactionId={row.id} title={row.counterparty_name||row.description||(row.direction==="income"?"Income":"Expense")} occurredOn={row.occurred_on} amountGross={Number(row.amount_gross)} currency={row.currency||"EUR"} evidence={row.evidence??[]}/><TransactionHistoryAction history={row.history??[]}/><button type="button" className={styles.iconButton} onClick={()=>dialog.current?.showModal()} aria-label="Edit transaction"><Pencil size={13}/></button><form action={deleteAction} onSubmit={e=>{if(!window.confirm(posted?"Delete this posted transaction? Zuelen will reverse it so the ledger remains auditable.":"Delete this transaction?"))e.preventDefault()}}><input type="hidden" name="source_transaction_id" value={row.id}/><button type="submit" className={`${styles.iconButton} ${styles.danger}`} disabled={deletePending}>{deletePending?<LoaderCircle className={styles.spin} size={13}/>:<Trash2 size={13}/>}</button></form><dialog ref={dialog} className={styles.dialog}><div className={styles.dialogCard}><div className={styles.dialogHead}><div><span>{posted?"Accounting-safe correction":"Edit transaction"}</span><h3>{posted?"Correct posted transaction":"Update transaction"}</h3></div><button type="button" onClick={()=>dialog.current?.close()}><X size={16}/></button></div>{posted?<p className={styles.notice}>Posted entries are never rewritten. Category changes create a reversal/replacement. Transaction fact edits use the existing safe correction workflow; VAT treatment itself is locked once posted.</p>:null}
 {posted&&eligible.length?<form action={categoryAction}><input type="hidden" name="source_transaction_id" value={row.id}/><label className={styles.full}><span>Accounting category</span><select name="account_code" defaultValue={accounts.find(a=>a.id===row.suggested_account_id)?.code??""} required><option value="">Choose a category…</option>{eligible.map(a=><option key={a.id} value={a.code}>{a.code} · {a.label}</option>)}</select></label><div className={styles.dialogFooter}><button type="submit" className={styles.secondary} disabled={categoryPending}>{categoryPending?<LoaderCircle className={styles.spin} size={14}/>:<RefreshCcw size={14}/>} Change category safely</button></div>{categoryState.status==="error"?<div className={styles.error}>{categoryState.message}</div>:null}</form>:null}
 <form action={editAction}><input type="hidden" name="source_transaction_id" value={row.id}/><div className={styles.grid}><label><span>Type</span><select name="direction" defaultValue={row.direction}><option value="expense">Expense</option><option value="income">Income / refund</option></select></label><label><span>Date</span><input name="occurred_on" type="date" defaultValue={row.occurred_on} required/></label>{!posted?<label className={styles.full}><span>VAT treatment</span><select name="vat_treatment" value={treatment} onChange={e=>setTreatment(e.target.value)}><option value="domestic">Luxembourg domestic VAT</option><option value="eu_b2b_reverse_charge">EU B2B · reverse charge</option><option value="non_eu">Non-EU / import evidence</option><option value="exempt_or_zero">Exempt / zero-rated</option><option value="unknown">Not sure · review later</option></select></label>:<input type="hidden" name="vat_treatment" value={defaultTreatment}/>}<label><span>Amount</span><input name="amount" type="number" min="0.01" step="0.01" value={amount} onChange={e=>setAmount(Number(e.target.value)||0)} required/></label><label><span>{treatment==="eu_b2b_reverse_charge"?"Notional VAT rate":"VAT rate"}</span><select name="vat_rate" value={rate} onChange={e=>setRate(Number(e.target.value))}><option value={17}>17%</option><option value={14}>14%</option><option value={8}>8%</option><option value={3}>3%</option><option value={0}>0%</option></select></label>{treatment==="domestic"?<label className={styles.full}><span>Does the amount include VAT?</span><select name="vat_included" value={included?"yes":"no"} onChange={e=>setIncluded(e.target.value==="yes")}><option value="yes">Yes · TTC / gross</option><option value="no">No · HT / net</option></select></label>:<input type="hidden" name="vat_included" value="no"/>}<div className={`${styles.notice} ${styles.full}`}><Calculator size={13}/> Net {money(calc.net)} · {treatment==="eu_b2b_reverse_charge"?"self-assessed VAT":"VAT"} {money(calc.vat)} · Cash {money(calc.gross)}</div><label><span>Customer / supplier</span><input name="counterparty_name" defaultValue={row.counterparty_name??""}/></label><label><span>Country</span><input name="counterparty_country" maxLength={2} defaultValue={row.counterparty_country??""} placeholder="LU / FR / US"/></label><label className={styles.full}><span>Description / reason</span><textarea name="description" defaultValue={row.description??""} placeholder="e.g. Google subscription refund, ACD tax instalment…"/></label></div>{editState.status==="error"?<div className={styles.error}>{editState.message}</div>:null}<div className={styles.dialogFooter}><button type="button" className={styles.secondary} onClick={()=>dialog.current?.close()}>Cancel</button><button type="submit" className={styles.primary} disabled={editPending}>{editPending?<LoaderCircle className={styles.spin} size={14}/>:<Pencil size={14}/>} {editPending?"Saving…":"Save details"}</button></div></form></div></dialog>{deleteState.status==="error"?<span className={styles.inlineError}>{deleteState.message}</span>:null}</div>;
}
